# Simple Job Scheduler
# Python >= 3.11 required
# No external dependencies - uses Python stdlib only
#
# Development / testing:
#   pip install pytest

# Runtime (Novel Genesis Pipeline) — optional, only needed for full pipeline runs
langgraph>=0.2
langchain-openai>=0.1

# Development / testing
pytest
