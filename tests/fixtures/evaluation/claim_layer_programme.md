# Claim Layer Programme Fixture

The Cloud-Migration Programme requires Security Certification before the production cutover can proceed.

The API Gateway may block Deployment if its readiness evidence remains implied rather than explicit.
